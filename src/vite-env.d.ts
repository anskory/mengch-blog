/**
 * Vite 环境类型声明文件
 *
 * 作用：
 * 为 Vite 提供的客户端 API 提供 TypeScript 类型支持，
 * 包括 import.meta.env 等环境变量相关的类型定义。
 *
 * 通过三斜杠指令引用 Vite 内置的类型定义，
 * 使得在 TypeScript 代码中可以正确使用 Vite 特有的 API。
 */

/// <reference types="vite/client" />
